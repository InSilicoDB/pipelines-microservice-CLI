
commands:
    pipeline:approve:
        description: 'Approve a pipeline release'
        arguments:
            base_url:
                mode: required
                description: 'The location of the pipeline microservice'
    pipeline:deny:
        description: 'Deny a pipeline release'
        arguments:
            base_url:
                mode: required
                description: 'The location of the pipeline microservice'
    pipeline:hide:
        description: 'Unpublish a pipeline'
        arguments:
            base_url:
                mode: required
                description: 'The location of the pipeline microservice'
    pipeline:publish:
        description: 'publish a pipeline'
        arguments:
            base_url:
                mode: required
                description: 'The location of the pipeline microservice'